query LeagueLayout($slug: String!) {
  league(slug: $slug) {
    id
    name
    slug
    divisions {
      id
      name
      session {
        id
        name
        startDate
        endDate
      }
    }
  }
}